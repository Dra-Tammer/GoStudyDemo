第一类题目-12+实习题-5-01,02
12. 排序综合
 利用随机函数产生N个随机整数（20000以上），对这些数进行多种方法进行排序。
要求：
1) 至少采用两种方法实现上述问题求解（提示，可采用的方法有插入排序、希尔排序、起泡排序、快速排序、选择排序、堆排序、归并排序）。
并把排序后的结果保存在不同的文件中。
2) 统计每一种排序方法的性能（以上机运行程序所花费的时间为准进行对比），找出其中较快的方法。
3) 如果采用3种或以上的方法者，可适当加分。
实习题：
5-01
如果矩阵A(m*n)中的某个元素a(ij)是第i行中的最小值，同时又是第j列中的最大值，则称此元素为该矩阵中的一个马鞍点。
假设以二维数组存储矩阵，试编写算法求出矩阵中的所有马鞍点。
5-02
已知全省有m个学生参加计算机等级考试，其成绩值均为0-100之间的整数值，且成绩中有很多值重复出现，统计各个分数值，重复出现的次数。
说明：
要求每道题目必须拥有设计界面展示本设计已实现的子功能，用户可以自由选择使用任一子功能，并且可以选择重复使用系统或者退出系统。
